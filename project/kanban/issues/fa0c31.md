---
id: fa0c31
title: "status: CLI parse tests for the merged surface"
type: chore
blocked_by: []
design: project
labels: []
created: 2026-07-19T02:24:09Z
disposition: accepted
disposition_at: 2026-07-19T04:03:29Z
---

## What to build

Add ~4 CLI parse tests for the merged `project status` surface (mirror the sandbox-domain parse tests), from the 577897 review:

- `project status set <id> <to>` routes to the Set subcommand
- `project status <id>` routes to the positional (detail), not a subcommand
- `project status <id> --lane <x>` is rejected (positional/filter conflict)
- `project show` is an unrecognized subcommand

## Acceptance criteria

- [ ] The four parse tests exist and pass; `cargo test` + clippy `-D warnings` green.


## Review notes

Implemented in sandbox `card-fa0c31` (fresh VM after widening the sandbox
allowlist with the crates.io origins — this build fetched normally, no `--offline`
workaround). Landed via `git apply --3way`; host-verified 333 tests + clippy + all
flake checks green. **Lighter-touch sign-off:** test-only card whose four cases
were specified by the independent 577897 reviewer, so the design already had an
independent set of eyes. Orchestrator-verified the tests are meaningful (real
`Cli::try_parse_from` assertions covering set-routing, positional detail, the
id/--lane conflict, and `show` being unrecognized), not rubber-stamp. Reserved the
full sandbox review for a9c135, which carries actual logic.
