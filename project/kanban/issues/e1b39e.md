---
id: e1b39e
title: Caution orchestrators against leading the reviewer
type: docs
blocked_by: []
labels: [PDD004, orchestration, skill]
created: 2026-08-10T16:34:46Z
---

## What to fix

The whole value of the `needs-review` state is that a second party reaches its
own conclusion. But the orchestrator writes the review directive, and an agent
is highly susceptible to suggestion. Nothing in `project-orchestration` warns
about this, and its own guidance nudges toward it: a good directive is told to
list "what should **block**".

Observed in `cdata/arcanabasis` while driving PDD038. The orchestrator wrote,
in the directive for card `457e37`:

> Recommend BLOCK if you conclude a criterion is unmet in substance. I would
> rather bounce this card than let a criterion quietly go unsatisfied twice.

The reviewer blocked. Its findings were substantiated — it traced a call chain,
proved two match arms were compiler-forced no-ops, read three sibling cards,
and found a mutation that survived 82 passing tests — so the verdict looks
sound. But it is no longer possible to say how much of it was independent
judgement.

The contrast makes the point sharper. Card `580353` had the same class of
defect, a `Capturable:` criterion satisfied by argument rather than by a
capture. Its directive did not invite a block, and its reviewer accepted. Two
reviewers, comparable evidence, opposite verdicts — and the variable that
differed most was the orchestrator's framing.

An accept/block record produced this way reads cleaner than it is.

## What to fix

Add a section to `project-orchestration` on writing a review directive that
does not lead:

- Name what to examine. Do not name the verdict you expect, and do not say
  which way you would rather it went.
- Ask questions rather than asserting findings. "Trace what spawns this
  payload" beats "nothing spawns this payload — confirm".
- Passing on a prior reviewer's finding is legitimate; passing on your own
  conclusion about the current card is not.
- Say plainly that the reviewer may accept, and that an accept is not a failure
  of the review.
- When the orchestrator has a real suspicion, put it in as a question among
  several, not as the centrepiece.

Also worth recording as a known hazard in the "Gotchas learned in practice"
list, next to the existing "reviewer != implementor is a hard rule" note: a
reviewer that is independent in name only is worth less than it appears, and
the orchestrator is the party best placed to compromise it without meaning to.

## Acceptance criteria

- [ ] `project-orchestration` has explicit guidance on not leading the reviewer
- [ ] The guidance distinguishes relaying a prior finding from asserting your own conclusion
- [ ] The existing "what should block" advice is reworded so it does not invite a predetermined verdict
- [ ] A worked example shows a leading directive and a neutral rewrite of it
- [ ] The hazard appears in the gotchas list

## Review notes

Reviewed by `review-e1b39e-1b7311c0` against branch tip `3d4d975`.
**VERDICT: accept**, all five criteria assessed individually and met.

- **AC 1** — new section "Writing a directive that does not lead"
  (`SKILL.md:148-211`): five named rules, a worked example, a gotchas entry.
  Judged actionable rather than merely descriptive.
- **AC 2** — rule 3 (`SKILL.md:167-171`): "Relaying a prior reviewer's finding
  is legitimate; relaying your own conclusion is not." The reviewer specifically
  assessed *operability* and found the test grounded in a concrete fact — did a
  prior review round happen — which an orchestrator can answer mid-session
  without weighing abstract categories.
- **AC 3** — verified against history rather than by reading the new text alone.
  The original at `98d6b45` (`SKILL.md:133`) read "failure modes, and what
  should **block**"; the rewrite (`SKILL.md:132-135`) reads "failure modes. Ask
  questions… rather than asserting findings… Name areas of uncertainty, not
  conclusions." Judged a genuine rewrite that does not keep the same pull in
  softer words.
- **AC 4** — the leading example (`SKILL.md:182-188`) reproduces all three
  failure modes from the card (asserted conclusion, preferred outcome,
  confirmation ask); the neutral rewrite (`SKILL.md:196-208`) converts each to
  question form and leaves the verdict open. The lead is genuinely removed.
- **AC 5** — gotchas entry at `SKILL.md:601-607`, naming the hazard, its danger
  (the accept/block record reads cleaner than it is), and cross-referencing the
  new section.

Markdown gates clean (`markdown lint` Prettier-clean, `markdown format` leaves
`SKILL.md` unchanged), run through the project menu. The reviewer used a
temporary worktree and removed it; nothing left running.

Minor observation, judged *not* a problem and therefore not filed: the
`report done` format template at `SKILL.md:146` still carries `would-you-block`,
while the neutral-rewrite example omits it. The reviewer's reasoning is that the
template asks for the **reviewer's own** verdict rather than the orchestrator's,
so it does not lead — an inconsistency of detail, not of principle.

**Directive self-assessment.** The review directive asked the reviewer to hold
that directive itself against the five new rules, and stated that a finding
against the orchestrator would be a legitimate result. It reported substantial
compliance: questions rather than assertions (rule 1), an explicit statement
that an accept is legitimate (rules 2 and 4), no relaying of the orchestrator's
own conclusion about this card (rule 3), and its specific checks embedded as one
of six independent questions rather than centred (rule 5). Useful evidence that
the guidance is followable in practice, not only readable.

## Dispatch log

- Implementor VM: `card-e1b39e-f1d07ed4` (launched 2026-08-10; paused warm after
  `done`).
- Reviewer VM: `review-e1b39e-1b7311c0` (launched 2026-08-10).
- Branch under review: `sandbox-guest/card-e1b39e-f1d07ed4`, tip `3d4d975`,
  delivered as `delivered/card-e1b39e-f1d07ed4`. Seed arrived board-clean (a
  single `dispatch seed` commit), as expected now that `12bead` has landed.
- Implementor's `done` summary: added a "Writing a directive that does not lead"
  subsection to `project-orchestration/SKILL.md` with five rules (name what to
  examine not the verdict; state no preference for a bounce; relay prior
  findings vs. assert your own; say an accept is valid; embed a suspicion as one
  question among several), a worked example pairing a leading directive with a
  neutral rewrite, and a new gotcha entry noting the orchestrator is the party
  best placed to compromise reviewer independence unintentionally. Also reworded
  the existing "what should block" bullet to ask questions rather than assert
  findings. Markdown format and lint clean.
- The review directive asks the reviewer to hold **the directive itself** to the
  rules the branch adds, and says a finding against the orchestrator is a
  legitimate result — the artifact under review governs the very instrument used
  to review it.

