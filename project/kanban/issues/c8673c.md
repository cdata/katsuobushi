---
id: c8673c
title: The dispatch guard is a backstop, not a routine catch
type: chore
blocked_by: [7f46b9]
labels: [PDD004, landing, sandbox]
created: 2026-08-09T09:00:01Z
---

## What to build

Say that the dispatch guard is a backstop.

Card `e7fd2b` added a guard that refuses a dispatch when agent-authored commits
would freeze. It exists because the old landing copied a guest commit and
repaired the author afterwards, and the next dispatch made that commit an
ancestor of a remote bookmark, after which the repair was refused. The guard
caught the routine case.

Once a landed commit is created by the owner (card `7f46b9`), there is no
routine case left. The guard keeps its value: it still catches work landed by
any other route. Its purpose changes from routine to backstop, and the code and
the skills should say so — otherwise a later reader deletes it as dead code, or
a reader who trips it looks for a re-authoring step that no longer exists.

Two things to do:

- Update the guard's comments and its refusal message so they describe what it
  now covers, and do not point at a procedure that has been removed.
- Update the skills that describe the guard for the same reason. Check the same
  places for the retired "re-author before the next dispatch" deadline and
  remove what is left of it.

Do **not** remove the guard.

## Acceptance criteria

- [ ] The guard is retained, and a comment says what it still covers and why it
      is no longer a routine catch
- [ ] The refusal message names a recovery an operator can still perform, and
      names no procedure that has been removed
- [ ] No skill or code comment still states that re-attribution must happen
      before the next dispatch
- [ ] Existing tests for the guard still pass, and any test name or comment that
      describes it as the routine path is corrected


## Review notes

Peer review by `review-c8673c-*` (independent VM): **VERDICT needs-changes on one
item — the commit prefix.** Everything functional was clean, verified rather than
assumed:

- **Guard logic untouched.** The diff is entirely comments and refusal-message
  text. `AGENT_EMAIL`'s value, `list_sandbox_guest_refs`' body (including the
  range bounding against `refs/remotes/sandbox-guest/`), `guard_against_agent_
  commits`' body, and the fail-closed behaviour on a git probe error are all
  unchanged. No behaviour smuggled into a wording card.
- **The refusal message is actionable.** It names how the commits arrived, says
  the situation is off-script, and gives two explicit recovery steps.
- **The "no skill changes needed" claim is accurate.** The reviewer independently
  grepped `plugins/katsuobushi/skills/` for re-attribution, `--reset-author`,
  `jj metaedit`, and deadline wording; the only hit describes why squash landing
  needs no repair, which is affirmative context, not a retired step.
- Gates green: 46 controller tests, 10 protocol tests, lint clean.

**Orchestrator override on the blocking item.** The reviewer required the branch
commit be reworded from `refactor(sandbox):` to `chore(sandbox):`. The finding is
real but its target moved: the convention in the `project` skill maps a card's
type to **the closing commit's** prefix, and under the squash landing introduced
by `7f46b9`, the closing commit is the orchestrator's landing commit composed
from the card title — not the guest's branch commit. The suggested fix would also
require amending the branch tip, which contradicts the "review rounds add
commits; the branch is never rewritten" rule that `fdc720` is documenting.

Landed at `ready` as signed commit `ea0ae95` with the correct `chore(sandbox):`
prefix, satisfying the convention where it actually applies. The branch retains
its non-conforming `refactor(...)` message as the revert artifact.

The squash conflicted only on `BOARD.md`, carried in the dispatch seed; resolved
in favour of the orchestrator's copy. The guest's own commit touched only
`dispatch.rs`.
