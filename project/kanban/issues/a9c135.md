---
id: a9c135
title: "status: follow-up nits from 394a3c review (rfc3339 range-check, cancelled test, accept-all+force)"
type: chore
blocked_by: []
design: project
labels: []
created: 2026-07-19T03:12:05Z
disposition: accepted
disposition_at: 2026-07-19T04:03:29Z
---

## What to build

Three minor, non-blocking nits from the 394a3c review:

- [ ] `parse_rfc3339` (clock.rs) range-checks H/M/S (reject hour 99 etc.) so a hand-corrupted-but-numeric timestamp falls through to hidden rather than parsing.
- [ ] Add a DIRECT set_status unit test asserting a *cancelled* transition stamps `disposition_at` (only accepted is asserted today; same code path).
- [ ] `project status set --accept-all --force`: `--force` is currently a silent no-op. Either reject the combo (conflicts_with) or document that --accept-all ignores it.

## Acceptance criteria

- [ ] All three addressed; `cargo test` + clippy `-D warnings` green.


## Review notes

Implemented in sandbox `card-a9c135` (built online cleanly on the widened
allowlist), landed via `git apply --3way`, host-verified 335 tests + clippy + all
flake checks green + `--accept-all --force` rejected. Peer-reviewed in sandbox
`review-a9c135` — **ACCEPT** (high test quality, no regressions): range-check
accepts all valid times incl. 23:59:59 and rejects out-of-range → falls through to
hidden; the cancelled test exercises the real update_note/FixedClock path; the
--force conflict is enforced both orderings and leaves --accept-all-alone and
set-with-force working; the 24h window boundary is untouched. Only out-of-scope
pre-existing parser nits (no day-vs-month validation), neither introduced nor
worsened — timestamps are machine-generated valid, so not worth a card.
