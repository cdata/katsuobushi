---
id: 335714
title: Harden the shipped heartbeat wiring against silent absence and stale docs
type: chore
blocked_by: []
labels: [PDD003, sandbox]
created: 2026-08-07T23:12:35Z
---

## What to build

Card `9689a8` shipped a library heartbeat set and its peer review confirmed the
checks work when run by hand inside a VM. The review also recorded four smaller
defects around the wiring. None blocked that card. Together they are worth one
pass.

- **Silent absence.** When `KATSU_SHIPPED_HEARTBEATS` is unset the server skips
  the shipped directory with no log line and no error (`server.rs:1071-1073`). A
  deployed VM always has the variable, so the failure is invisible exactly where
  it is most confusing — a non-Nix development environment, where the protection
  the card exists to guarantee is simply absent and nothing says so.
- **A doc comment that is wrong.** `runner.rs:34` says `run_heartbeat_set`
  "Never returns normally". It spawns its tasks and returns; the *inner* tasks
  are the ones that never return.
- **Undefined-looking label collision.** A project heartbeat carrying the same
  label as a shipped one is not deduplicated. Both run and both appear in the
  work state. That follows from "a project directory adds to the shipped set, it
  never replaces it", so the behaviour is right — but it is neither documented
  nor tested, and a reader would reasonably expect the project file to win.
- **A latent Nix build failure.** The `*.yaml` glob at `default.nix:357` would
  fail the build if the shipped heartbeat directory ever became empty.

## Acceptance criteria

- [ ] A server started with `KATSU_SHIPPED_HEARTBEATS` unset says so on stderr,
      once, naming what is missing and what it means.
- [ ] `run_heartbeat_set`'s doc comment describes what the function actually
      does.
- [ ] The label-collision rule is stated where a heartbeat author will read it,
      and a test pins it.
- [ ] An empty shipped heartbeat directory does not fail the Nix build.
- [ ] A test covers an empty `hb_dirs`.
- [ ] `clippy -D warnings` is clean.


## Dispatch log

- implementor: `card-335714-53e7edb2`

Round 1 — implementor reported `done`, addressing all four defects.
`server.rs` now emits a clear stderr message naming `KATSU_SHIPPED_HEARTBEATS`
and its effect when the variable is unset, instead of silently skipping the
shipped directory. `run_heartbeat_set`'s doc comment replaces "Never returns
normally" with "Runs the rescan loop indefinitely", matching the style of
`run_one` and `run_turn_heartbeat`. The label-collision rule is documented on
`collect_heartbeats` and pinned by two new tests — an empty `hb_dirs` slice, and
two same-label heartbeats from different directories both surviving the merge.
`default.nix` replaces the `*.yaml` glob in `shippedHeartbeatsDir` with a `find`
so an empty shipped directory cannot fail the build.

Host verified the merged tree: `nix develop -c rust test` passes **579**
(459 + 64 + 46 + 10) and the flake still evaluates. Landed on the `850ce4` tip
with no conflict.

## Review notes

### Round 1 — `review-335714-1c438525` — VERDICT: needs-changes

Gates: 579 tests pass (459 + 64 + 46 + 10, matching the host), lint clean.

**BLOCKING — the label-collision rule does not reach an author.** It is
documented only in the `collect_heartbeats` Rust doc comment
(`runner.rs:37-41`). The sandbox skill's "Authoring heartbeats" section
(`SKILL.md:468-551`) says nothing about it. The criterion is explicit — *"stated
where a heartbeat author will read it"* — and an author writing
`.katsuobushi/heartbeats/` YAML reads the skill, not Rust internals. The
reviewer's point is sharper than mere placement: an author who gives a project
heartbeat the same label as a shipped one will likely expect **override**
semantics, since that is the common layered-config convention, and will instead
get both running with nothing to tell them otherwise.

**Everything else passed:**

- **The unset-variable message fires exactly once.** It sits at
  `server.rs:1347-1351` inside `main()`, ten lines *before* the
  `tokio::spawn(run_heartbeat_set(...))` at `:1358`, and the rescan loop lives
  entirely inside that function (`runner.rs:96-119`). Structurally unreachable
  on a rescan — the host's spam concern does not apply.
- **The message is useful** — it names the variable, states the consequence
  ("shipped heartbeats will not run") and gives the corrective action.
- **The Nix `find` fix is reproducible.** Each file is copied independently to a
  deterministic path, so traversal order has no effect on the output. Confirmed
  by building the derivation.
- **Both new tests are solid** —
  `it_keeps_both_heartbeats_when_labels_collide_across_directories` asserts both
  `len() == 2` **and** that two entries carry the shared label, so it genuinely
  proves both survive rather than asserting a length.

**Note on the doc-comment finding:** the reviewer judged the *old* wording
("Never returns normally") to have become accurate again, because `612108` gave
`run_heartbeat_set` its own `loop {}` — it is now the loop owner, not merely a
spawner. The original defect had gone stale between being filed and being fixed.
The new wording is still preferred as more informative.

### Round 2 — implementor revision

Added the rule to the sandbox skill's "Authoring heartbeats" rules list
(`SKILL.md:501-504`): project heartbeats add to the shipped set and never
replace it; a project heartbeat sharing a shipped label does **not** override
it — both run and both appear in the work state; use a distinct label when a
single entry is wanted. No code, tests or Nix changed, and the implementor said
so rather than skipping the Rust gates silently.

Host verified the placement and wording, and that it corrects the *expectation*
rather than only describing the mechanism — which was the reviewer's actual
point. `markdown lint` clean, no conflicts.

Landing hit the `sandbox fetch` orphaning bug a sixth time, with a conflicted
working copy; recovered with `jj op restore` → `jj abandon` → re-fetch → rebase
forward.

### Round 2 — `review-335714-1c438525` — VERDICT: needs-changes

The rule now reaches the authoring section, reads naturally in the rules list,
keeps the section tight, and does dismantle the override expectation — an author
who assumed override reads "both run" and knows they were wrong.

**BLOCKING — but it replaces that false belief with a different one.** The claim
"both appear in the work state" is **wrong**. The coordinator
(`server.rs:1142-1158`) keeps a `HashMap<String, StoredBeat>` keyed by **label**
and calls `beats.insert(update.label, ...)` on every incoming update.
`HashMap::insert` **overwrites** when the key exists.

So two same-label heartbeats each get their own `JoinHandle` — keyed by file
path, so both genuinely *run* their checks — but both send updates under the
same label string, and the coordinator holds only **one slot per label**.
Last-received wins. The work state shows **one** entry for that label, not two.

The same "both appear" wording sits in the `collect_heartbeats` doc comment
(`runner.rs` ~`:41`). That was cleared as out of scope in round 1, but
propagating it into the **authoring** section is not acceptable, because authors
act on the authoring section.

Round-1 clearances all confirmed unregressed; `markdown lint` passes; Rust gates
correctly not required for a SKILL.md-only commit; `project/kanban/` untouched.

### Round 3 — implementor revision

Read `server.rs:1142-1158` first and confirmed the mechanism independently:
`beats` is a `HashMap<String, StoredBeat>` keyed by label, and `:1158` calls
`beats.insert(update.label, ...)`, which overwrites on collision — one slot per
label.

Corrected the claim in both places. `SKILL.md:501-505` now reads: *"…both run,
but the work state holds one slot per label, so whichever update arrives last
masks the other. Use a distinct label so both results are visible."* The
`collect_heartbeats` doc comment (`runner.rs:37-42`) carries the same
correction.

That wording states the consequence an author should act on — one heartbeat's
result **masks** the other's — rather than only describing the mechanism, which
was the point of the finding.

Gates: `markdown lint` clean, `rust lint` clean.

### Round 3 — `review-335714-1c438525` — VERDICT: accept

Gates: `markdown lint` pass, `rust lint` clean.

The new wording is **mechanically accurate** — the coordinator holds one
`HashMap` slot per label and last-write-wins on every insert. On the host's
question about flapping: the reviewer agreed that "masks" does not fully capture
the observable behaviour when two same-label heartbeats run on **different**
intervals, where the displayed entry alternates rather than settling. But the
claim is **not false**, and the takeaway an author acts on — one slot per label,
use distinct labels to see both — is correct and complete. Accepted on that
basis rather than bounced for a word.

No `both appear` claim survives in `SKILL.md` or the `collect_heartbeats` doc
comment. The one remaining instance at `runner.rs:392` is in a **test** doc
comment about distinct-label merging, where it is accurate. The distinct-label
advice was verified true against `server.rs`.

No regression in the round-1 clearances; `project/kanban/` untouched.
