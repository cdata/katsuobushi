---
id: c5c2c2
title: Retire progressStallSecs and the host stall notice
type: chore
blocked_by: [5e545d]
labels: [PDD003, sandbox, rust]
created: 2026-08-07T21:13:55Z
---

## What to build

The work state answers what the stall notice was guessing at. The knob's right
value depended on a project's cold-build time, which is the class of guess this
design exists to remove. This repo runs the library default of
`progressStallSecs ? 300` (`lib/sandbox/default.nix:124`) and passes no override
from `flake.nix`, so the host printed a stall notice about five minutes into
every cold build.

Remove `progressStallSecs` from the library arguments and the host spec, and
remove the stall notice from the host watchdog
(`rust/katsuobushi-controller/src/sandbox/prompt.rs`).

The independent check does not disappear with the knob. It moves into the
heartbeat contract: a heartbeat that always beats reaches its own declared
timeout and becomes late.

## Acceptance criteria

- [ ] A cold build produces no stall notice.
- [ ] `progressStallSecs` appears in neither the library arguments nor the host
      spec.
- [ ] The stall-notice path is gone from the host watchdog, along with any test
      that only covered it.


## Added scope (from the `a91d4c` review)

While retiring `progressStallSecs` from `lib/sandbox/default.nix`, also fix the
stale comment a few lines above it. `lib/sandbox/default.nix:262` still says an
unreported turn "resolves as `ended-unreported`" once the auto-nudge budget is
spent. Card `a91d4c` retired that state — it is now `idle`. The comment is inert,
but it sits in the library that configures every sandbox and documents behaviour
that no longer exists.

- [ ] No occurrence of `ended-unreported` survives in `lib/`.

## Dispatch log

- implementor: `card-c5c2c2-51c06eaa`

Round 1 — implementor reported `done`. Removed the knob from
`lib/sandbox/default.nix` (the argument, the NB comment, the `katsuctlSpec`
inheritance), from the Rust `Spec` struct and its spec tests, and from the host
watchdog in `prompt.rs` — `Watchdog.progress_deadline`, `DriveEvent::Stalled`,
`stall_notice_at`, `stall_message`, `STALL_NOTICES`, the `select!` stall timer
arm and every stall-only test. Also dropped `ReportKind::Stalled` from
`output.rs` and `progress_stall_secs` from the attach / fetch / screenshot /
start / status fixture specs. Fixed the stale `ended-unreported` comment as
directed.

Host verified: `ended-unreported` no longer appears anywhere in `lib/` (the
added-scope criterion), and `rust test` is green on the merged tree.

**Gap found by the host, referred to the reviewer:** `progressStallSecs` still
appears three times in `lib/sandbox/README.md` (`:73` in the argument table,
`:175` in a worked example, `:317` in prose telling the reader to set it past
their cold-build time). The card's criterion names "the library arguments" and
"the host spec", so the letter may be satisfied — but the README is the human
guide for this library, and it now documents a knob that no longer exists.
Anyone following `:317` would write a removed argument into their flake and get
an evaluation error.

## Review notes

### Round 1 — `review-c5c2c2-79884288` — VERDICT: needs-changes

Gates observed: `rust test` 46/46, `rust lint` clean, `markdown lint` clean,
flake evaluates. **The code removal is correct — documentation is the blocker.**

1. **The README references ARE in scope — BLOCKING.** The reviewer was invited
   to reject the host's reading and did not: `lib/sandbox/README.md:73` *is* the
   authoritative argument table, `:175` a copyable example, `:317` an explicit
   action directive. Beyond those three lines, the whole **"The progress notice"
   section (`:295-320`)** documents a two-notice escalation the watchdog no
   longer has and tells the reader to tune the window. Its ruling: the narrow
   reading "could technically hold, but it produces an absurd result — the
   removal is 'done' while the library's own human guide still tells readers to
   set a non-existent knob."
2. **`MIGRATING.md` is actively harmful — BLOCKING.** Section 0.3.6
   (`:144-148`) carries a live example setting `progressStallSecs = 1500` and
   instructs the reader to "set the new `progressStallSecs` argument past your
   build time". Anyone who followed the official upgrade path now gets a hard
   Nix evaluation error, with no new section announcing the removal.
   `CHANGELOG.md` has no removal entry either.

**Cleared:**

- **No non-stall watchdog behaviour was damaged.** The reviewer read the whole
  `drive()` `select!` loop (`prompt.rs:689-753`): the stream arm (heartbeats,
  reports, lifecycle), the heartbeat-deadline arm and the delivery-deadline arm
  are intact and unmodified. `last_prog` and `stalls` belonged exclusively to
  the removed arm. Loop termination unchanged. The stop grace and auto-nudge
  timers live in the guest crate and were untouched.
- **All four deleted tests were stall-only**, and journal coverage for the
  surviving events (`Report`, `Stopped`, `ReArmed`) is fully retained by
  `the_drive_sink_journals_reports_stops_and_rearms` (`:1803-1841`).
- **The enum removal is clean.** `grep -rn 'Stalled' rust/` returns nothing; the
  `journal_event` match is exhaustive; no serialized form ever wrote `stalled`
  to `turn-state.json`. `render_rearmed` now uses `ReportKind::Stopped`, which
  renders identically, and the JSON `event` field is set explicitly rather than
  derived — no behavioural change.
