---
id: 9689a8
title: Ship a library heartbeat set that covers a bare repo
type: feature
blocked_by: [51d5d7]
labels: [PDD003, sandbox]
created: 2026-08-07T21:12:51Z
disposition: accepted
disposition_at: 2026-08-10T02:00:10Z
---

## What to build

A repo that has never heard of heartbeats still gets the protection. The library
ships its own heartbeats inside the VM image, as ordinary files in the same
format that a project uses. A project's `.katsuobushi/heartbeats/` directory
**adds** to the shipped set. It never replaces it.

The shipped set covers the local work that every observed stall actually was:

- **`agent-work`** — the `katsuobushi-agent` cgroup consumed CPU since the last
  beat.
- **`nix-build`** — the `nix-daemon` cgroup consumed CPU since the last beat.

Both matter. One cold build through `nix develop` runs the agent under
`systemd.services.katsuobushi-agent` and its derivations under
`nix-daemon.service`, so the work of one build appears in two cgroups.

These files are what people copy, so they are also the worked examples. Shell
inside a block scalar is indented, and that is the usual source of a check that
passes and means nothing. The shipped files must show the indentation and the
exit-status rule clearly.

An operator who asks why a VM is `active` must be able to read the answer out of
a file inside the VM, not out of Rust.

## Acceptance criteria

- [ ] A workspace with no `.katsuobushi/heartbeats/` directory still reports
      `active` for the length of a cold build.
- [ ] The shipped heartbeats are readable files inside the VM, in the same
      format a project uses.
- [ ] A workspace with one heartbeat of its own still runs the shipped set —
      both a shipped heartbeat and a project heartbeat appear in the state.
- [ ] `agent-work` beats while the `katsuobushi-agent` cgroup burns CPU.
      `nix-build` beats while the `nix-daemon` cgroup burns CPU.
- [ ] Each shipped file carries a comment showing the block-scalar indentation
      and the exit-zero rule.


## Dispatch log

- implementor: `card-9689a8-fe2e77a2`

Round 1 — implementor reported `done`. Shipped `agent-work.yaml` and
`nix-build.yaml` under `rust/katsuobushi-sandbox-guest/heartbeats/`, each
checking whether the `katsuobushi-agent` or `nix-daemon` cgroup consumed CPU
since the last beat, with state files under `/run/katsuobushi/control/`. The
NixOS guest module builds a `shippedHeartbeatsDir` derivation, installs it at
`/etc/katsuobushi/heartbeats/` via `environment.etc`, and exposes it as
`KATSU_SHIPPED_HEARTBEATS`. The server now builds a `Vec<PathBuf>` of the
shipped dir (always) plus the project dir (only when it exists) and passes both
to `run_heartbeat_set` via a new `collect_heartbeats` helper.

**Conflict reconciled on the host by the orchestrator.** This card and `0e8753`
both refactored `run_heartbeat_set` in the same commit range, so the rebase
produced a 2-sided conflict in `runner.rs` and `server.rs` — two hunks each.
Both were signature unions rather than competing intents:

- `run_heartbeat_set` now takes **both** changes:
  `(hb_dirs: Vec<PathBuf>, cwd: PathBuf, turn_armed: Arc<AtomicBool>)`, loading
  via `collect_heartbeats` and still spawning the turn heartbeat.
- The `heartbeat` import merged to carry both `HeartbeatError` (this card) and
  `TurnBeat` (`0e8753`).
- The `server.rs` call site keeps this card's shipped-plus-project `hb_dirs`
  construction and passes `0e8753`'s `turn_armed`.
- Both doc comments and both inline comments were merged rather than one
  dropped.

Host verified the merged tree before handing it to review: 37 + 38 tests pass.
The independent reviewer is asked to check the reconciliation specifically,
since the orchestrator wrote it.

## Review notes

### Round 1 — `review-9689a8-83a076ab` — VERDICT: accept

Gates observed by the reviewer: 38/38 tests pass, clippy clean.

**The orchestrator's conflict resolution was checked first and is CORRECT.**
Both intents survived: `run_heartbeat_set` (`runner.rs:39`) takes this card's
`hb_dirs`/`cwd` and the sibling's `turn_armed`; the import (`runner.rs:14-16`)
carries both `HeartbeatError` and `TurnBeat`; the `server.rs:1078` call site
passes the shipped-plus-project dirs from lines 1071-1077 together with the
`turn_armed` flag from line 1028. No feature was silently dropped.

**The shipped checks were run by hand inside a real VM — they mean something.**

- `agent-work`: the `katsuobushi-agent.service` cgroup exists and `cpu.stat`
  carries `usage_usec` in the expected form. First run writes the baseline and
  exits 1 (**no spurious first beat**); the second exits 0 once CPU advanced.
- `nix-build`: with `nix-daemon` inactive the cgroup is absent and the check
  exits 1 immediately. After `nix develop` the cgroup appears, the `awk`
  extraction works, and an **idle** daemon correctly yields no beat.

Neither check is always-zero or always-non-zero — the failure mode the card
called out by name does not apply.

Add-not-replace confirmed: `collect_heartbeats` (`runner.rs:20-29`) only
appends, shipped first then project when `is_dir()`. The Nix module is sound —
`shippedHeartbeatsDir` (`default.nix:355`), `environment.etc` install
(`:1190`), `KATSU_SHIPPED_HEARTBEATS` (`:1152`) reaching the server via
systemd → claude → MCP inheritance, and `/run/katsuobushi/control` created
0700 agent-owned so the `.prev` writes succeed. Both YAML files carry the
block-scalar indentation comment and the exit-status rule; someone copying
either would write a correct heartbeat.

**Minor issues recorded, none blocking — filed separately:**

- `KATSU_SHIPPED_HEARTBEATS` unset silently skips the shipped set with no log
  or error. Always set in a deployed VM, but a real gap outside Nix.
- `runner.rs:34` says "Never returns normally", which is wrong — the function
  spawns and returns; the *inner* tasks never return.
- A project heartbeat sharing a shipped one's label is not deduplicated; both
  run. Consistent with "never replaces", but undocumented and untested.
- The Nix `*.yaml` glob (`default.nix:357`) would fail the build if the shipped
  directory ever became empty. Latent.
- A `nix-daemon` restart costs one missed beat (new cgroup counter vs stale
  `.prev`). One tick, acceptable.
