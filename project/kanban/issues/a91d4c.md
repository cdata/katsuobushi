---
id: a91d4c
title: Combine the heartbeats into one work state and retire ended-unreported
type: feature
blocked_by: [51d5d7, 0e8753]
labels: [PDD003, sandbox, rust]
created: 2026-08-07T21:13:05Z
disposition: accepted
disposition_at: 2026-08-10T02:00:10Z
---

## What to build

The guest combines every heartbeat into one **work state**. The rule is a
logical OR over the set, read in one precedence order:

```
  finished   the agent ran a terminal report for this turn
     >
  active     one or more heartbeats beat
             (late)  a beating heartbeat is past its own timeout
     >
  idle       nothing beats
```

- `finished` outranks every observation. The agent's own terminal report is
  authoritative about the turn. An agent that reports `done` while a build still
  runs reads as `finished`; the leftover process is not allowed to hide
  completed work. The risk is guarded already — the `project-orchestration`
  skill tells an orchestrator to trust the branch and not the VM.
- `late` is **not** a fourth state. It is a flag on `active`. A heartbeat past
  its timeout is by definition still beating; one that stops is not late but
  absent.
- `idle` is the floor. It means nothing runs, and it carries no accusation.
- `finished` belongs to one turn. A new prompt clears it, and the state returns
  to whatever the heartbeats say.
- The grace window after a stop (`stopGraceMs = 1500`) is unchanged. It still
  absorbs a terminal report that arrives just after the stop hook, so a clean
  finish is never misread as a silence.

**`ended-unreported` leaves the vocabulary.** The state it named is now `idle`,
and the cases it confused are now separate.

## Acceptance criteria

- [ ] A terminal report during a beating heartbeat yields `finished`.
- [ ] A heartbeat past its timeout beside a healthy one yields `active` with the
      late flag raised.
- [ ] No beat yields `idle`.
- [ ] A new prompt clears `finished`, and the state returns to what the
      heartbeats say.
- [ ] A terminal report that arrives inside the 1500 ms grace window after a
      stop still yields `finished`.
- [ ] `ended-unreported` appears nowhere in the guest, the protocol, or the host.
- [ ] The precedence rule is decided in a pure core and unit-tested without
      timers, BDD-named — e.g. `it_prefers_finished_over_a_beating_heartbeat`,
      `it_flags_late_without_leaving_active`, `it_falls_back_to_idle`.


## Dispatch log

- implementor: `card-a91d4c-c0c19fce`

Round 1 — implementor reported `done`. Added a pure `combine_work_state(finished,
beats)` to `heartbeat.rs` mapping a terminal-report flag plus every `BeatStatus`
onto `Finished > Active { is_late } > Idle`, with ten BDD-named precedence
tests. Removed `Phase::EndedUnreported` from the guest state machine
(`server.rs`) and the host reader (`liveness.rs`); grace expiry with no terminal
report now resolves to `Phase::Idle`. Renamed the affected tests across
`server.rs`, `liveness.rs` and `status.rs`. Landed cleanly on the `9689a8` tip
with no conflict.

Host verified the scrub: `ended-unreported` and `EndedUnreported` appear nowhere
under `rust/`. One stale **comment** survives outside the Rust crates at
`lib/sandbox/default.nix:262`, which still describes a turn resolving "as
ended-unreported" after the auto-nudge budget is spent. Not functional code, but
it now documents behaviour that no longer exists. Flagged to the reviewer.

## Review notes

### Round 1 — `review-a91d4c-628c7423` — VERDICT: accept

Gates observed by the reviewer: 85 guest tests, 447 controller tests, clippy
clean on both.

- **Precedence is correct** (`heartbeat.rs:365-374`):
  `finished > active(is_late) > idle`. The subtle case holds — `is_late` is
  gated on `b.beating && b.is_late`, so a late heartbeat beside a healthy one
  still raises the flag, and the flag never appears on a non-active state.
- **Test renames lost no strength.** The `liveness.rs` rename *pivoted* to
  testing `ended-ok` deserialization rather than weakening; the `server.rs`
  renames changed only the expected `Phase` value. Verified against the diff.
- **Grace window unchanged** — `STOP_GRACE_MS_DEFAULT = 1500`
  (`protocol/src/lib.rs:147`); a terminal report inside the window still
  resolves `EndedOk`.

**The `EndedUnreported` removal does collapse one real distinction**, and the
reviewer named it rather than waving it through: `status` no longer carries an
*age* for an unreported turn closure — `turn N idle` instead of
`turn N ended-unreported 14m ago`. The mechanism is the `is_ended()` change at
`liveness.rs:155`. This is intentional per the design, which says the state
`ended-unreported` named is now `idle` and the cases it confused are now
separate, but the lost age is worth knowing about.

**`combine_work_state` has no production callers.** It is exercised only by its
ten unit tests; `server.rs` never calls it and `WorkState` is never reported to
the host. So two acceptance criteria — "a terminal report during a beating
heartbeat yields `finished`" and "a new prompt clears `finished`" — hold at the
pure-function level but are **not verifiable end-to-end yet**. The reviewer
asked for an integration card; `f7de81` (*Report and persist every work-state
transition*) already is that card and is blocked on this one, so no new card was
filed. Accepted on that basis.

The stale `ended-unreported` comment at `lib/sandbox/default.nix:262` is inert.
The criterion names the guest, the protocol and the host — met if "host" means
the Rust crates, not met if it reaches Nix config. Folded into `c5c2c2`, which
already edits that region.
