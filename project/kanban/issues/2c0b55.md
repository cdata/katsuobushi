---
id: 2c0b55
title: Close deliver's test gaps against the fetch standard
type: chore
blocked_by: []
labels: [sandbox]
created: 2026-08-09T09:30:43Z
---

## What to build

`deliver.rs` ships 7 module tests against `fetch.rs`'s 16. The reviewer of card
69d1df judged them meaningful — the exact call-sequence test matches the
`fetch.rs` standard — but found gaps:

- the instance-not-found error path is not exercised at the `deliver` layer
  (it is delegated to `resolve.rs`)
- index-based instance resolution is not exercised in the `deliver` context
- no test covers a branch whose middle segments contain slashes, e.g.
  `refs/heads/feature/my-work` deriving `delivered/my-work`
- one test uses `contains()` assertions where an exact match would pin the
  behaviour

Found by the peer reviewer of card 69d1df; not blocking that card.

## Acceptance criteria

- [ ] The three untested scenarios above are covered
- [ ] Weak `contains()` assertions are tightened to exact matches where the
      behaviour is exactly specified

