---
id: a8a212
title: Stop prescribing foreground or background in the agent contract
type: docs
blocked_by: [87286c]
labels: [PDD003, sandbox]
created: 2026-08-07T21:13:55Z
disposition: accepted
disposition_at: 2026-08-10T02:00:10Z
---

## What to build

The contract stops teaching the agent how to do its work. Foreground and
background are the agent's choice.

The prescription leaves both places that carry it today:

- the agent contract in `lib/sandbox/default.nix` (lines 565 to 570), which
  tells the agent to run long commands in the foreground;
- the dispatch instructions in `project/kanban/.dispatch-instructions.md`
  (lines 16 to 18), which tell it to start the cold build in the background.

The two sides disagree with each other, and neither is right. A rewording toward
foreground blocking does not work: one Bash call caps at 600 seconds unless
`BASH_MAX_TIMEOUT_MS` raises it, nothing in `lib/sandbox/default.nix` sets it,
and a 15-minute build therefore has no foreground path. After this design
nothing needs one.

What the contract keeps:

- A terminal report is required to return work to the host. This is unchanged.
- Incremental reports are offered as an option, not an obligation.

## Acceptance criteria

- [ ] A search of both files finds no rule preferring foreground or background.
- [ ] A search of both files finds the terminal-report requirement, unchanged.
- [ ] Incremental reports read as an option in both files, not an obligation.
- [ ] Neither file tells the agent to `report working` before it yields.


## Dispatch log

- implementor: `card-a8a212-d352883d`

Round 1 — implementor reported `done`. In `lib/sandbox/default.nix` it deleted
the two bullets directing agents to run long commands in the foreground and
forbidding backgrounding, along with the `report working` obligation attached to
them, keeping the terminal-report requirement intact. In
`project/kanban/.dispatch-instructions.md` it removed "in the background" from
the cold-build instruction and dropped "Never end your turn with a background
build still running".

Host verified: the only surviving "foreground" mentions are two unrelated Nix
comments (`default.nix:116` on how heartbeat ticks work, `:1853` on `handOff`
running `katsuctl` not-`exec`) — neither is agent-facing. The terminal-report
requirement is present in both files, and the one remaining `report working`
line reads "optional progress while you work".

Editing `.dispatch-instructions.md` is in scope here: the host-only board rule
covers `BOARD.md` and the `<id>.md` card notes, and this card names that file
directly. No card note or `BOARD.md` was touched.

## Review notes

### Round 1 — `review-a8a212-0173017a` — VERDICT: accept

Gates observed by the reviewer: `markdown lint` green, and the flake still
evaluates (`nix develop -c menu` succeeded). No Rust changed, so the Rust gates
were correctly not required — the reviewer said so rather than skipping
silently.

- **Both surviving `foreground` mentions are internal implementation comments**
  (`default.nix:116`, `:1853`), not agent-facing text. The host's reading is
  confirmed.
- **Nothing unrelated was deleted** — this was the real risk in a
  deletion-shaped card, and the reviewer checked each rule individually. The
  terminal-report bullet was promoted verbatim into inline prose; the cold-build
  warning keeps its substance (start once, no contention, wait); the
  one-simple-command rule, the never-write-the-board invariant and the
  commit/push discipline are untouched.
- **The terminal report is still unambiguous.** Operative sentence, quoted:
  *"Before you stop, always run exactly one terminal report — `report done` if
  the work is complete and pushed, or `report blocked` if you cannot proceed."*
  It cannot be read as permission to end a turn silently, which was the way this
  relaxation could have gone wrong.
- **`report working` is explicitly optional** (`:565`), satisfying the third
  criterion.
- **Both files read coherently after the cuts** — no dangling prose, no lists
  left shorter than their introduction claims.
