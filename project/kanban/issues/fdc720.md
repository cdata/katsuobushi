---
id: fdc720
title: Give the reviewer the branch under review
type: docs
blocked_by: [69d1df, 9cc042]
labels: [PDD004, sandbox]
created: 2026-08-09T09:00:00Z
---

## What to build

Document how a reviewer gets the branch it reviews.

A branch is the unit under review. It is the review artifact and the revert
artifact, it carries the writer's name, and it keeps that name. The reviewer
reads **that branch**, not the owner's integrated tip — because nothing of the
card is in the owner's history until the review passes.

A guest sees exactly one host directory, so a reviewer cannot fetch from the
party that wrote the change. The orchestrator moves the branch into the
reviewer's mirror with the delivery verb from card `69d1df`, and the reviewer
fetches it there.

Write the peer-review protocol in
`plugins/katsuobushi/skills/project-orchestration/SKILL.md` to say:

- Which command the orchestrator runs, and when in the loop it runs.
- What the reviewer does in its own clone to read the branch, and how it names
  the branch tip.
- That a review round **adds** commits to the branch and never rewrites it, so
  the reviewer can answer "what changed since I last read this" with a range.
- What a re-review of a bounced card needs: the orchestrator delivers the
  updated branch again before the reviewer reads it.

This replaces the raw `git push` into another instance's storage that the
orchestrator has been doing by hand.

## Acceptance criteria

- [ ] The reviewer's workspace holds the branch under review, and the reviewer
      can name the branch tip and read its commits
- [ ] The orchestrator gives a reviewer the branch with one documented command
      and no raw `git push`
- [ ] The skill states that a review round adds commits and never rewrites the
      branch
- [ ] The skill gives the range a reviewer uses to read only what changed since
      the previous round
- [ ] The re-review path after a bounce is documented


## Dispatch log

- Implementor VM: `card-fdc720-f12a39fa` (paused warm)
- Reviewer VM: `review-fdc720-*` (see sandbox status)
- Branch under review: `review/card-fdc720-f12a39fa`, relevant commit `6a4d439`
- The board files in the branch's diffstat are dispatch-seed carry-through, not
  agent writes. The guest's own commit touches only
  `plugins/katsuobushi/skills/project-orchestration/SKILL.md` — the
  orchestrator-only board invariant held.
- Orchestrator found two probable defects before review and sent both to the
  reviewer to confirm or refute: the documented `sandbox deliver ...` command
  errors with `Unknown subcommand: deliver` (the verb is only reachable as
  `katsuctl sandbox --config <spec> deliver`), and the reviewer read command
  `git log delivered/<inst>` is missing its `origin/` remote prefix.

## Review notes

Peer review by `review-fdc720-bb7f4e11` (independent VM): **VERDICT
needs-changes, would block.** Markdown lint and format clean. Both defects the
orchestrator raised were confirmed independently.

**1. The documented command does not exist (blocking).** `sandbox deliver`
returns `Unknown subcommand: deliver`. The reviewer traced it: `deliver` is
absent from the subcommand list in `lib/sandbox/default.nix`, which wires only
`start`, `dispatch`, `prompt`, `status`, `fetch`, `stop`, `attach`, and
`screenshot`. The card's criterion "one documented command" fails on a command
that errors, so this blocks `fdc720` — but the reviewer places the root cause on
`69d1df`, which added the Rust verb and the skill docs but missed the Nix
wrapper, and states that gap must be fixed there before this card's text can be
accurate.

**2. The reviewer read commands are missing `origin/` (blocking).** Tested
directly: `git log delivered/x` fails, `git log origin/delivered/x` succeeds.
Both forms in the new section — the plain read and the `<prev-tip>..` range —
need the prefix.

**3. The placeholder is wrong (non-blocking).** `review-<card-id>` should be
`review-<card-id-instance>`; `--name` appends a random suffix at launch, so an
operator substituting literally gets a name that does not resolve.

Judged correct: deleting the old "you do not need to commit to get it reviewed"
sentence. Under the delivery protocol the implementor must commit, so the removed
claim was actively misleading.
