---
id: 612108
title: Re-read the heartbeat set after startup so a fixed file is picked up
type: feature
blocked_by: [51d5d7]
labels: [PDD003, sandbox, rust]
created: 2026-08-07T22:22:28Z
disposition: accepted
disposition_at: 2026-08-10T02:00:10Z
---

## What to build

The runner from card `51d5d7` loads the heartbeat set exactly once, when the
guest starts, and prints each parse error once at that moment. That satisfies the
design's rule that a broken heartbeat reaches the host once rather than once per
interval — the peer reviewer confirmed the letter of the contract is met and
judged the boundary acceptable for that card.

The consequence is the part worth deciding on purpose. A heartbeat file that is
added, edited or **fixed** after the guest starts is invisible until the guest
restarts. An author who corrects a typo in a `check` body sees no change and has
no signal explaining why.

Settle which reading of "once" the design intends:

- **once ever** — the current behaviour. Then the gap is a documentation problem:
  say plainly that the set is read at startup, and that a change needs a restart.
- **once per error state** — report a broken file once, stay silent while it stays
  broken, and re-evaluate when it changes. Then the runner needs to notice a
  changed directory without reintroducing per-interval error spam.

Prefer the second reading only if it can be done without a watcher that
complicates the runner. Re-reading on a slow timer, or on an explicit signal, may
be enough.

## Acceptance criteria

- [ ] The chosen reading is stated in the design notes, not only in code.
- [ ] If re-reading is implemented: a heartbeat file corrected after startup
      begins beating without a guest restart.
- [ ] A file that stays broken still produces exactly one error, not one per
      interval and not one per re-read.
- [ ] A newly added heartbeat file is picked up under the same rule as a
      corrected one.
- [ ] The behaviour is unit-tested in the pure core, without timers.


## Dispatch log

- implementor: `card-612108-23fcd589`

Round 1 — implementor reported `done`, and chose the **"once per error state"**
reading. Added `source: PathBuf` to `Heartbeat` so files can be tracked across
scans, plus a pure `HeartbeatSetState` / `HeartbeatSetDiff` / `eval_scan` core
in `heartbeat.rs` with seven unit tests. Replaced the one-shot load in
`run_heartbeat_set` with a 60-second rescan loop that spawns tasks only for
newly added or newly fixed files. Documented the chosen reading in the PDD.

Diffstat: PDD003 (+18), `heartbeat.rs` (+156), `runner.rs` (+65/-23).

Host verified the merged tree: `nix develop -c rust test` passes **565**
(455 + 54 + 46 + 10). Landed on the `87286c` tip with no conflict.

Note this card's implementor edited `project/design/`, which is correct here —
the first acceptance criterion required the chosen reading to be stated in the
design notes and not only in code. The board (`project/kanban/`) was untouched,
as required.

## Review notes

### Round 1 — `review-612108-4f889f07` — VERDICT: needs-changes

Gates observed by the reviewer: 565 tests pass, clippy clean, markdown lint
clean. (The commit message claimed 110 tests — that is one binary's count; 565
is the workspace total.)

**BLOCKING — a deleted heartbeat file leaks its task forever**
(`runner.rs:93-96`, `heartbeat.rs:207`). `state.loaded` only ever grows; paths
are never removed, `run_one` is an unconditional infinite loop, and the
`JoinHandle` from `tokio::spawn` is dropped immediately, so no cancellation path
exists. Deleting a heartbeat file leaves its old check body running until the
guest restarts — a removed heartbeat is unremovable. This is the mirror image of
the bug this card was written to fix, and the rescan loop is what makes it
newly surprising: the design now promises to notice file changes, but only ever
in the additive direction.

**Findings, non-blocking:**

1. **The state key is the path, not the path plus the error**
   (`heartbeat.rs:245-249`). A broken file edited into a *different* broken
   state is suppressed, because the path is already in `state.broken`. So the
   implemented rule is "once per broken **path**", not "once per error state" as
   the card, the code and the PDD all now claim. The name overstates the
   granularity.
2. **A working file's content changes are silently ignored**
   (`heartbeat.rs:238`). Once a path is in `state.loaded` it is never re-read,
   so editing a valid check body leaves the old task running the old command.
   Structurally the same gap as the deletion bug.
3. **PDD cross-reference.** The added section (PDD003 lines 190-206) accurately
   describes the code and reads in the document's voice. But the pre-existing
   bullet at line 183 still says "the guest reports the error to the host once"
   with no qualification, so a reader of that line alone still meets the
   ambiguous "once".

**Confirmed working:** a persistently broken file does **not** re-report on each
rescan (`heartbeat.rs:246`, pinned by `it_reports_a_broken_file_exactly_once`);
no double-spawn (`it_does_not_respawn_an_already_running_heartbeat`); the 60s
interval's first tick fires at t=0, so initial load is not delayed; and adding
`source: PathBuf` is a clean change that leaves the parser tests passing.

### Round 2 — implementor revision

Added `to_stop: Vec<PathBuf>` to `HeartbeatSetDiff`. `eval_scan` now uses
`retain()` to detect paths that left the valid set (deleted **or** newly
broken), adds them to `to_stop` and removes them from `state.loaded`.
`run_heartbeat_set` keeps a `HashMap<PathBuf, JoinHandle<()>>` and `abort()`s
the handle for each stopped path. Test
`it_stops_a_loaded_heartbeat_when_its_file_is_deleted` covers deletion,
sustained absence and recreation.

Renamed "once per error state" to "once per broken path" in the `eval_scan`,
`HeartbeatSetState` and `run_heartbeat_set` docs and in PDD003, with a note that
a file edited between two broken states does not re-report. Added the
valid→valid known-limitation paragraph and the line-183 cross-reference.
Extended `it_reports_a_broken_file_exactly_once` to a third scan.

Host verified: `nix develop -c rust test` passes **566** (455 + 55 + 46 + 10),
`abort()` is wired at `runner.rs:95-97`, and "once per error state" no longer
appears in the PDD. The implementor's report was accurate this time and named
the command that produced its number.

Landing hit the `sandbox fetch` orphaning bug again (the one filed as its own
card): the import abandoned the rebased commits and conflicted the board files.
Recovered with `jj op restore` → `jj abandon` → re-fetch → rebase forward, as
before.

### Round 2 — `review-612108-4f889f07` — VERDICT: accept

Gates observed by the reviewer: `rust test` 565 green, `rust lint` clean,
`markdown lint` clean.

**Finding 1 (was blocking) — resolved, and the fix was itself audited.**
`abort()` lands at one of `run_one`'s three await points. At the ticker — the
typical case, since checks are short relative to their interval — it fires
cleanly with no child process. If it lands *inside* `timeout(budget,
child.wait())`, the `tokio::process::Child` is dropped without `kill_on_drop`,
so the `kill_pgroup` path is bypassed and the shell plus any grandchildren run
as orphans.

The reviewer judged that risk **real but bounded, and a net improvement**: the
pre-fix behaviour was a task running forever and spawning new checks
indefinitely; the new behaviour is at most **one** orphaned child for at most
one budget duration, in its own process group where it cannot affect the guest.
The gold-standard fix — `kill_on_drop(true)` on the `spawn_check` and
`spawn_detail` command builders — is filed separately rather than bolted on
here.

- **The `JoinHandle` map does not leak.** `handles.remove(&path)` runs before
  `abort()`, so entries are cleaned up as tasks stop.
- **Stopping newly-broken files as well as deleted ones is correct.** A file
  edited into a syntax error is no longer a valid heartbeat; stopping it is
  consistent with deletion and draws attention to the break, whereas silently
  running a stale body would be harder to debug. The PDD states "deleted or
  becomes unparseable", which matches.
- **Recreation works** — the path is removed from `state.loaded` by `retain()`,
  so a recreated file is treated as new. Pinned by the fourth-scan assertion in
  `it_stops_a_loaded_heartbeat_when_its_file_is_deleted`.

Findings 2, 3 and 4 all resolved: no "once per error state" survives; the PDD's
valid→valid limitation exactly matches `eval_scan`'s behaviour; and line 183 now
carries the cross-reference. `it_reports_a_broken_file_exactly_once` covers
three scans. No regressions, no new findings, `project/kanban/` untouched.

Minor unresolved bookkeeping: the reviewer counted 565 tests where the host
counted 566, differing by one in the guest lib binary. Immaterial to the
verdict — both observed the new deletion test present and passing, and both
gates green.
