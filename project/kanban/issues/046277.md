---
id: 046277
title: Dead is_work_idle state and stale nudge comments remain
type: chore
blocked_by: []
labels: [PDD004, sandbox]
created: 2026-08-11T02:15:41Z
---

## What to fix

Raised by the independent reviewer of card `3fd9ec` as two non-blocking
findings, accepted alongside it and filed together here because they are one
unit of work: leftovers from removing the idle-transition nudge path.

`3fd9ec` removed the `is_work_idle` guard from `GraceExpired` and deleted the
`nudge_pending` field and the `WorkStateIdle` nudge-firing path. Two artifacts
of the old design survive:

- **Dead state.** `is_work_idle` in the guest server is still *set* but never
  *read*. Its doc comment still describes the behaviour it used to gate.
- **Stale prose.** A comment in `lib/sandbox/default.nix` still describes the
  old idle-transition behaviour, which no longer exists.

Neither affects runtime behaviour, which is why the reviewer did not block.

## Why it matters

Dead state that is still maintained is a trap for the next reader: it looks
load-bearing, so a future change may try to preserve its semantics, or wire new
logic onto a flag that no longer means anything. The stale comments actively
describe behaviour the code no longer has, which is worse than no comment.

## What to fix

- Remove `is_work_idle` if nothing reads it, along with the code that maintains
  it — or, if it is worth keeping for diagnostics, say so explicitly in its doc
  comment and make that its stated purpose.
- Update the doc comment either way.
- Correct the `lib/sandbox/default.nix` comment to describe the current
  nudge behaviour.
- While there, check for any other comment in the guest server or the sandbox
  library that still describes the pre-`3fd9ec` idle-transition design.

## Acceptance criteria

- [ ] `is_work_idle` is either removed or documented with its actual current purpose
- [ ] No comment in the guest server describes the removed idle-transition nudge behaviour
- [ ] The `lib/sandbox/default.nix` comment matches the current nudge behaviour
- [ ] The workspace gate stays green

## Review notes

Reviewed by `review-046277-0f1b4d06` against branch tip `5822ac4`.
**VERDICT: accept**, all four criteria PASS.

- **AC 1** — the field, the `WorkStateIdle`/`WorkStateActive` `Event` variants,
  their `Session::step` match arms, and the coordinator dispatch block feeding
  them are all gone. No reference remains.
- **AC 2** — a full sweep of `server.rs` finds zero occurrences of
  "idle transition", "fires on idle", `is_work_idle`, `WorkStateIdle`,
  `WorkStateActive`, "real silence" or "build timer". Module docs, the
  `GraceExpired` handler comment and all remaining test comments describe the
  unconditional grace-timer model.
- **AC 3** — `default.nix` now reads "Nudges fire unconditionally on the grace
  timer — the agent is re-prompted even if a background build is still running —
  so the budget is always spent", replacing the old idle-transition text.
- **AC 4** — workspace gate green.

**Work-state telemetry is intact** (the orchestrator's main concern, since this
is the instrument that exposed the stall behind `3fd9ec`).
`run_work_state_coordinator` is untouched: it still aggregates heartbeats via
`combine_work_state`, emits `GuestMessage::WorkStateTransition` to the host on
every change, and writes `work-state.json` (forced on transitions, throttled to
~1s otherwise). `write_work_state`, `WorkStateRecord`, and every field
`sandbox status` reads — `workState`, `carryingLabel`, `carryingDurationSecs`,
`nudgeCount` — are unchanged. Only the bridge into the session machine was
removed.

**The two deleted tests removed subjects, not coverage.**
`it_does_not_fire_an_extra_nudge_on_work_state_idle` asserted a no-op for an
event `3fd9ec` had already stripped of side effects; the event is now gone, so
the test has no subject. `it_does_not_refill_the_budget_when_work_resumes` drove
`WorkStateActive`, which no longer exists — and its underlying guarantee (the
budget is never reset mid-turn) now holds by construction, there being no
mid-turn reset path at all.

**`3fd9ec`'s regression guards are unweakened.** Both
(`it_nudges_even_when_work_is_active` and
`an_unreported_turn_with_active_work_still_gets_nudged`) keep identical
substantive assertions — `inject_nudge=Some((1,1))`,
`schedule_grace=Some((1,Nudge))`, `nudgeCount=1`, `phase=InFlight`; only
comments were reworded.

**Test count reconciled at last.** Pre-card `c447ffd`: 522+64+48+10 = **644**.
Post-card `5822ac4`: 522+64+46+10 = **642**, all passing, lint clean. The −2 is
precisely the two deleted tests, with no other crate affected. The implementor's
"46/46" was the guest-server binary crate alone — the third per-crate figure
reported as a workspace total in this thread.

Nothing left running.

## Dispatch log

- Implementor VM: `card-046277-da6d75e4` (launched 2026-08-10).

